# Interdire l'accès au dossier tools
Order Deny,Allow
Deny from all

# Permettre uniquement en local (développement)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REMOTE_ADDR} !^127\.0\.0\.1$
    RewriteCond %{REMOTE_ADDR} !^::1$
    RewriteRule ^(.*)$ - [F,L]
</IfModule>
